#include <stdint.h>
#include <stdio.h>
#include <locale.h>

/* Задание:
* 
* Дана сторока из 4 десятичных цифр: "abcd".
* Если (a * d) % 2 == c:
*   e = min(c, d)
*   return "abed"
* Иначе:
*   return "abdc"
*/


int main() {
    return 0;
}